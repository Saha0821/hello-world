<!DOCTYPE html>
<html>

<head>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <style>
        body {
            margin-right: 650px;
            margin-left: 34px;
            background-color: #d6eaf0f3;
        }
        /* Include the padding and border in an element's total width and height */
        
        * {
            box-sizing: border-box;
        }
        /* Remove margins and padding from the list */
        
        ul {
            margin: 11;
            padding: 0;
        }
        /* Style the list items */
        
        ul li {
            cursor: pointer;
            flex: 50%;
            position: relative;
            padding: 13px 50px 12px 40px;
            list-style-type: none;
            background: #eee;
            font-size: 18px;
            transition: 0.2s;
            /* make the list items unselectable */
            -webkit-user-select: none;
            -moz-user-select: none;
            -ms-user-select: none;
            user-select: none;
        }
        /* Set all odd list items to a different color (zebra-stripes) */
        
        ul li:nth-child(odd) {
            background: #abf5ff;
        }
        
        ul li:nth-child(even) {
            background: #def6ff
        }
        /* Darker background-color on hover */
        
        ul li:hover {
            background: rgb(255, 255, 255);
        }
        /* When clicked on, add a background color and strike out text */
        
        ul li.checked {
            background: rgb(0, 63, 78);
            color: rgb(4, 176, 199);
            text-decoration: line-through;
        }
        /* Add a "checked" mark when clicked on */
        
        ul li.checked::before {
            content: '';
            position: absolute;
            border-color: rgb(170, 225, 250);
            border-style: solid;
            border-width: 0 2px 2px 0;
            top: 10px;
            left: 16px;
            transform: rotate(45deg);
            height: 15px;
            width: 7px;
        }
        /* Style the close button */
        
        .close {
            position: absolute;
            right: 0;
            top: 0;
            padding: 12px 16px 12px 16px;
        }
        
        .close:hover {
            background-color: #43d1f5;
            color: rgb(24, 21, 21);
        }
        /* Style the header */
        
        .header {
            background-color: #90e7f3f3;
            color: rgb(6, 6, 7);
            text-align: center;
            padding-bottom: 50px;
            padding-left: 12px;
            position: absolute;
        }
        /* Clear floats after the header */
        
        .header:after {
            content: "";
            display: table;
            clear: both;
        }
        /* Style the input */
        
        input {
            margin: 0;
            border: none;
            border-radius: 2px;
            width: 75%;
            padding: 10px 0px 10px 10px;
            float: left;
            font-size: 16px;
        }
        /* Style the "Add" button */
        
        .addBtn {
            padding: 10px;
            padding-left: 10px;
            padding-right: 25px;
            width: 25%;
            background: #0c4752;
            color: rgb(182, 240, 250);
            float: left;
            text-align: center;
            font-size: 16px;
            cursor: pointer;
            transition: 0.3s;
            border-radius: 0;
        }
        
        .addBtn:hover {
            background-color: rgb(121, 212, 240);
        }
        
        textarea {
            color: #000303;
            background: #90e7f3f3;
            border: solid 2px #000303;
            text-align: center;
            margin-right: 10px;
            margin-top: 35px;
            margin-left: 638px;
        }
        
        div.row {
            display: flex;
        }
        
        div.myDIV {
            flex: 50%;
            height: 215px;
        }
        
        div.header {
            flex: 50%;
            width: 600px;
            margin-top: 35px;
            margin-left: 4px;
            margin-bottom: 5px;
        }
    </style>
</head>

<body>
    <div class="row">
        <div class="header">
            <h2> My To Do List</h2>
            <input type="text" id="myInput" placeholder="Add a Task...">
            <span onclick="newElement()" class="addBtn">Add</span>
        </div>
        <div class="myDIV">
            <textarea name="Notes" rows="10" cols="50" placeholder="Journal Of the Day"></textarea>
        </div>

    </div>
    <ul id="myUL">
        <li class="checked">Refresh</li>
        <li>Plan for today</li>
        <li>Yoga</li>
    </ul>




    <script>
        // Create a "close" button and append it to each list item
        var myNodelist = document.getElementsByTagName("LI");
        var i;
        for (i = 0; i < myNodelist.length; i++) {
            var span = document.createElement("SPAN");
            var txt = document.createTextNode("\u00D7");
            span.className = "close";
            span.appendChild(txt);
            myNodelist[i].appendChild(span);
        }

        // Click on a close button to hide the current list item
        var close = document.getElementsByClassName("close");
        var i;
        for (i = 0; i < close.length; i++) {
            close[i].onclick = function() {
                var div = this.parentElement;
                div.style.display = "none";
            }
        }

        // Add a "checked" symbol when clicking on a list item
        var list = document.querySelector('ul');
        list.addEventListener('click', function(ev) {
            if (ev.target.tagName === 'LI') {
                ev.target.classList.toggle('checked');
            }
        }, false);

        // Create a new list item when clicking on the "Add" button
        function newElement() {
            var li = document.createElement("li");
            var inputValue = document.getElementById("myInput").value;
            var t = document.createTextNode(inputValue);
            li.appendChild(t);
            if (inputValue === '') {
                alert("You must write something!");
            } else {
                document.getElementById("myUL").appendChild(li);
            }
            document.getElementById("myInput").value = "";

            var span = document.createElement("SPAN");
            var txt = document.createTextNode("\u00D7");
            span.className = "close";
            span.appendChild(txt);
            li.appendChild(span);

            for (i = 0; i < close.length; i++) {
                close[i].onclick = function() {
                    var div = this.parentElement;
                    div.style.display = "none";
                }
            }
        }
    </script>

</body>

</html>
